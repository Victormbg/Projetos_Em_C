#include <stdio.h>

int main() {
  float veta[10], vetb[b];
  int i;

  for (i = 0; i < 10; i++) {
    scanf(� % f�, &veta[i]);
    b[i] = veta[i] * veta[i];
  }
  for (i = 0; I < 10; i++) {
    printf(� % f % f�, veta[i], vetb[i]);
  }
}
