#include <stdio.h>

int main() {
  int vet[30], i;

  for (i = 0; i < 30; i++) {
      scanf(�%d�, &vet[i];
  }
  for (i = 9; i < 25; i++) {
    if (vet[i] > 15 && vet[i] < 50) {
      printf(� % f�, vet[i]);
    }
  }
}
